import HeaderContainer from "./container/header.container";

const Header = () => {
  return (
    <header className="w-full bg-white min-h-16 shadow flex">
      <HeaderContainer></HeaderContainer>
    </header>
  );
};

export default Header;
