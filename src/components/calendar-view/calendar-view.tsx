import CalendarViewContainer from "./container/calendar-view.container";

const CalendarView = () => {
  return (
    <section className="calendarView">
      <CalendarViewContainer></CalendarViewContainer>
    </section>
  );
};

export default CalendarView;
